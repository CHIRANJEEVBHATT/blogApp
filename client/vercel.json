{
  "installCommand": "cd client/blogApp && npm ci",
  "buildCommand": "cd client/blogApp && npm run build",
  "outputDirectory": "client/blogApp/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}